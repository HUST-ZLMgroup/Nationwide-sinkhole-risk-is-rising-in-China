# Data availability notes

This repository stores code only. The workflow expects local geospatial source data, extracted predictor tables, trained GWR artifacts and generated prediction or optimization outputs under `data/` and `outputs/`.

The final manuscript should cite the authoritative data repository or archive separately. Do not commit restricted source data or large generated rasters directly to GitHub.
